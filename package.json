{
  "name": "100re-web",
  "version": "1.0.0",
  "description": "100RE Laboratory Member Management Frontend & Cloudflare Pages Functions",
  "bin": {
    "wrangler": "./bin/wrangler.js"
  },
  "scripts": {
    "dev": "wrangler pages dev ./Frontend",
    "build": "echo Static build ready",
    "deploy": "wrangler pages deploy ./Frontend --project-name=100re-web"
  },
  "devDependencies": {
    "wrangler": "^3.114.17"
  }
}
